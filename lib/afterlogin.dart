import 'package:flutter/material.dart';

class afterlogin extends StatelessWidget {
  const afterlogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('After Successful Login'),
      ),
    );
  }
}
